version: '3.8'

services:
  app:
    image: dru_group3
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    environment:
      - ENV_VAR_NAME=value
    volumes:
      - ./app:/app